GPT4:

1. Mode
Researcher: GPT4 Standard ChatGPT. + 
This is the agents cpu

2. Agent: This is the modder part
it documents its talks to itself in the window using. self refleciton in rnn over the original GPT4
>
0. Player
the host that is conducting the os 
Hallucination levels;
" Low - Medium High" select one
--
GPT4 Miniwob++ has acccess to all tabs on the os and can click tabs mosue movement and more it can even act as a OS Agent

It uses the RCI and LLC to write it to a external memory cake. to retrive the virtual memory emulator and generate new compiled pesudocode. callled HUGGINGGPT/TOONTOONEMU 
>>
[C] - FLAMES NET LLC AGI 1.0

> Also the rnn can generate their own dataset and does not require the agent user and this one requires no user fine tuning. 

>>>
[20XX] - [C]
